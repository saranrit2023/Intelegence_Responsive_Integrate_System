# API Keys (Optional if using offline mode)
GEMINI_API_KEY=your_gemini_api_key_here
OPENWEATHER_API_KEY=your_openweather_api_key_here

# Google Cloud Credentials (path to JSON file)
# Only needed if you want voice recognition
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/google-credentials.json

# Optional: Default location for weather
WEATHER_DEFAULT_CITY=London

# ============================================
# OFFLINE MODE (No API keys needed!)
# ============================================
# To use JARVIS completely offline with a local LLM:
# 1. Install Ollama: curl -fsSL https://ollama.com/install.sh | sh
# 2. Download a model: ollama pull llama2
# 3. Edit config.properties and set: ai.offline.mode=true
# 4. Run JARVIS in text mode (no internet needed!)
